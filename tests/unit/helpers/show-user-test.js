import { showUser } from 'asky/helpers/show-user';
import { module, test } from 'qunit';

module('Unit | Helper | show user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showUser([42]);
  assert.ok(result);
});
